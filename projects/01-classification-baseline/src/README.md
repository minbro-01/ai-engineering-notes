# src

이 폴더에는 프로젝트에서 사용하는 코드를 둡니다.

예상 파일 예시:

```text
load_data.py      # 데이터 로딩과 전처리
train.py          # 모델 학습
evaluate.py       # 평가 지표 계산
utils.py          # 공통 함수
```

처음에는 하나의 파일로 시작해도 괜찮지만, 코드가 길어지면 역할별로 나눕니다.
